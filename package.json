{
	"private": true,
	"workspaces": ["packages/*"],
	"scripts": {
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"test": "bun run --filter '*' test",
		"build": "bun run --filter '*' build"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/bun": "^1.3.9",
		"typescript": "^5.6.0"
	}
}
