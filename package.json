{
	"private": true,
	"workspaces": ["packages/*"],
	"scripts": {
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"test": "bun run --filter '*' test",
		"build": "bun run --filter '*' build"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/bun": "^1.3.9",
		"@types/diff": "^8.0.0",
		"diff": "^8.0.4",
		"typescript": "^5.6.0"
	}
}
